namespace home_rental_tool.Domain
{
    public enum MembershipLevel { PayAsYouGo, Basic, Plus, Pro, Contractor }
    public enum ToolTier { Tier1 = 1, Tier2, Tier3, Tier4, Tier5 }
    public enum TimeWindow { FourHours, Day, Weekend, Week }
    public enum InsurancePlan { Basic, Standard, Premium, Profi }
}
